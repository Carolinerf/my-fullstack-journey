[project]
name = "01-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carolinerf", email = "holmes00h@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.16.1",
    "jupyter>=1.1.1",
]

[project.scripts]
01-python = "01_python:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
