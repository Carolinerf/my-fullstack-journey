[project]
name = "chat-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Carolinerf", email = "holmes00h@gmail.com" }]
requires-python = ">=3.12"
dependencies = ["ollama>=0.6.2"]

[project.scripts]
chat-cli = "chat_cli.chat_cli:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "GPT",
]
